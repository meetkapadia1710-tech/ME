# Agent Rules & Guidelines

## Deletion & Cleanup Protocol
- **Explicit Target Scope**: "Cleanup" or "refactoring" instructions must ONLY remove files explicitly named in the user prompt.
- **No Unsolicited File Deletions**: Never infer additional deletions or remove files that appear unused (e.g., utility scripts, mock data generators, schema seeds) without flagging them for explicit user confirmation first.
